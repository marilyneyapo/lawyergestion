<form method="post" action="{{ path('app_typecolab_delete', {'id': typecolab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typecolab.id) }}">
    <button class="btn">Delete</button>
</form>
